/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { KloudlessAuthenticatorDirective } from './kloudless-authenticator.directive';

describe('KloudlessAuthenticatorDirective', () => {
  it('should create an instance', () => {
    const directive = new KloudlessAuthenticatorDirective();
    expect(directive).toBeTruthy();
  });
});
