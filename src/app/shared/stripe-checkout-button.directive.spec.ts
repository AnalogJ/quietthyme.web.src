/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { StripeCheckoutButtonDirective } from './stripe-checkout-button.directive';

describe('StripeCheckoutButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new StripeCheckoutButtonDirective();
    expect(directive).toBeTruthy();
  });
});
